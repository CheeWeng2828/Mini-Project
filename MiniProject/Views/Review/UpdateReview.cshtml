@model UpdateReviewVM
@{
    ViewBag.Title = "Update Review";
}
@* 

//addsound
<audio id="notifySound" src="~/Sounds/ding-36029.mp3" preload="auto"></audio> *@

@* <form asp-action="UpdateReview" method="post" class="form" id="updateForm">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

    <table class="form-table">
        <tr>
            <td>Hotel</td>
            <td><b>@Model.HotelName</b></td>
            <input asp-for="HotelName" type="hidden" />
            <input asp-for="Id" type="hidden" />
            <input asp-for="HotelId" type="hidden" />
            <input asp-for="BookingId" type="hidden" />
        </tr>
        <tr>
            <td>Comment</td>
            <td>
                <textarea asp-for="Comment" class="form-control" rows="4"></textarea>
                <span asp-validation-for="Comment" class="text-danger"></span>
            </td>
        </tr>
        <tr>
            <td>Service Rating</td>
            <td>
                <select asp-for="ServiceRating" class="form-control">
                    @for (int i = 1; i<=5; i++)
                    {
                        <option value="@i" selected="@(Model.ServiceRating==i ? "selected" : null)">@i</option>
                    }
                </select>
                <span asp-validation-for="ServiceRating" class="text-danger"></span>
            </td>
        </tr>
        <tr>
            <td>Cleanliness Rating</td>
            <td>
                <select asp-for="CleanlinessRating" class="form-control">
                    @for (int i = 1; i<=5; i++)
                    {
                        <option value="@i" selected="@(Model.CleanlinessRating==i ? "selected" : null)">@i</option>
                    }
                </select>
                <span asp-validation-for="CleanlinessRating" class="text-danger"></span>
            </td>
        </tr>
        <tr>
            <td>Overall Rating</td>
            <td>
                <select asp-for="Rating" class="form-control">
                    @for (int i = 1; i<=5; i++)
                    {
                        <option value="@i" selected="@(Model.Rating==i ? "selected" : null)">@i</option>
                    }
                </select>
                <span asp-validation-for="Rating" class="text-danger"></span>
            </td>
        </tr>
    </table>

    <section>
        <button type="submit">Update</button>
        <button type="reset" class="reset-btn">Reset</button>
        <button class="back-btn" data-get="/Review/ListByBooking?bookingId=@Model.BookingId">Back</button>
    </section>
</form> *@

@* @section foot {
    <script>
        const form = document.getElementById('updateForm');
        const sound = document.getElementById('notifySound');

        form.addEventListener('submit', function (e) {
             // check validility
            const validator = $(form).data('validator');
            if (validator && !$(form).valid()) {
               //no valid->no sound
                e.preventDefault();
                return;
            }

            e.preventDefault();
            sound.currentTime = 0;
            sound.play().then(() => {
                setTimeout(() => form.submit(), 250);
            }).catch(() => form.submit());
        });
    </script>
} *@
